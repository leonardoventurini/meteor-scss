# export TEST_BROWSER_DRIVER=puppeteer

function @lib-test() {
  meteor --release=3.0-rc.2 test-packages ./ --driver-package meteortesting:mocha --full-app
}

function @lib-release() {
  meteor publish --release=METEOR@3.0-rc.2
}
